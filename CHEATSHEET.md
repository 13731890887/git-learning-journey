# Git 命令速查表

打印这个页面，放在键盘旁边！🖨️

---

## 🔥 最常用命令

```bash
git status              # 查看状态（使用频率最高！）
git add .               # 添加所有修改
git commit -m "信息"     # 提交
git push                # 推送到远程
git pull                # 拉取并合并
```

---

## 📁 基础操作

| 命令 | 说明 |
|------|------|
| `git init` | 初始化仓库 |
| `git clone URL` | 克隆远程仓库 |
| `git status` | 查看状态 |
| `git add 文件` | 添加到暂存区 |
| `git add .` | 添加所有修改 |
| `git commit -m "信息"` | 提交 |
| `git commit -am "信息"` | 添加并提交（仅已跟踪文件） |

---

## 🔍 查看历史

| 命令 | 说明 |
|------|------|
| `git log` | 查看详细历史 |
| `git log --oneline` | 简洁显示（一行一个） |
| `git log --graph` | 图形化显示分支 |
| `git show` | 显示最后一次提交 |
| `git diff` | 查看未暂存的修改 |
| `git diff --staged` | 查看已暂存的修改 |
| `git diff HEAD~1` | 和上一个提交比较 |

---

## 🌿 分支操作

| 命令 | 说明 |
|------|------|
| `git branch` | 查看所有分支 |
| `git branch 分支名` | 创建分支 |
| `git switch 分支名` | 切换分支 |
| `git switch -c 新分支` | 创建并切换 |
| `git checkout 分支名` | 旧版切换分支 |
| `git checkout -b 新分支` | 旧版创建并切换 |
| `git merge 分支名` | 合并分支 |
| `git branch -d 分支名` | 删除分支 |
| `git branch -D 分支名` | 强制删除 |

---

## 🔄 远程操作

| 命令 | 说明 |
|------|------|
| `git remote -v` | 查看远程仓库 |
| `git remote add origin URL` | 添加远程仓库 |
| `git push -u origin main` | 首次推送 |
| `git push` | 推送 |
| `git pull` | 拉取并合并 |
| `git fetch` | 拉取但不合并 |
| `git pull --rebase` | 变基方式拉取 |

---

## ↩️ 撤销操作

| 命令 | 说明 |
|------|------|
| `git restore 文件` | 撤销工作区修改 |
| `git restore --staged 文件` | 取消暂存 |
| `git restore --source=HEAD~1 文件` | 恢复到上一个版本 |
| `git reset HEAD 文件` | 取消暂存（旧版） |
| `git checkout -- 文件` | 撤销工作区修改（旧版） |
| `git commit --amend` | 修改最后一次提交 |
| `git reset --soft HEAD~1` | 撤销提交，保留修改 |
| `git reset --hard HEAD~1` | 撤销提交，删除修改 |
| `git revert 提交ID` | 创建新提交来撤销 |

---

## 🏷️ 标签操作

| 命令 | 说明 |
|------|------|
| `git tag` | 查看所有标签 |
| `git tag v1.0` | 创建标签 |
| `git tag -a v1.0 -m "信息"` | 创建带注释的标签 |
| `git push origin v1.0` | 推送标签 |
| `git push origin --tags` | 推送所有标签 |
| `git checkout v1.0` | 切换到标签 |

---

## 📝 暂存操作

| 命令 | 说明 |
|------|------|
| `git stash` | 暂存当前修改 |
| `git stash save "信息"` | 暂存并添加说明 |
| `git stash list` | 查看暂存列表 |
| `git stash apply` | 应用最近的暂存 |
| `git stash pop` | 应用并删除暂存 |
| `git stash drop` | 删除暂存 |
| `git stash clear` | 清空所有暂存 |

---

## 🔧 高级操作

| 命令 | 说明 |
|------|------|
| `git rebase main` | 变基到 main |
| `git cherry-pick 提交ID` | 挑选某个提交 |
| `git blame 文件` | 查看每行是谁修改的 |
| `git reflog` | 查看所有操作记录 |
| `git clean -fd` | 清理未跟踪的文件 |
| `git gc` | 垃圾回收，优化仓库 |

---

## 💡 组合使用

### 撤销最后一次提交但保留修改
```bash
git reset --soft HEAD~1
```

### 修改最后一次提交信息
```bash
git commit --amend
```

### 修改最后一次提交内容
```bash
git add 文件
git commit --amend --no-edit
```

### 暂存当前工作，切换分支
```bash
git stash
git switch other-branch
git stash pop
```

### 撤销文件的本地修改
```bash
git restore 文件名
```

---

## ⚠️ 危险操作（谨慎使用）

| 命令 | 说明 | 风险 |
|------|------|------|
| `git reset --hard HEAD` | 清空所有未提交修改 | 💀💀💀 |
| `git reset --hard HEAD~1` | 删除最后一次提交 | 💀💀💀 |
| `git clean -fd` | 删除所有未跟踪文件 | 💀💀 |
| `git branch -D 分支` | 强制删除分支 | 💀💀 |

---

## 🎯 工作流程示例

### 日常开发流程
```bash
git status                    # 查看状态
git add .                     # 添加修改
git commit -m "功能完成"       # 提交
git pull                      # 拉取更新
git push                      # 推送
```

### 新功能开发
```bash
git switch -c feature-xxx     # 创建功能分支
# ... 开发 ...
git add .                     # 添加修改
git commit -m "添加功能"       # 提交
git switch main               # 切回主分支
git pull                      # 拉取更新
git merge feature-xxx         # 合并功能
git push                      # 推送
git branch -d feature-xxx     # 删除功能分支
```

### 修复冲突
```bash
git merge feature-xxx         # 合并时出现冲突
# 编辑文件，解决冲突
git add 冲突文件              # 标记为已解决
git commit                    # 完成合并
```

---

## 📱 获取帮助

```bash
git help 命令          # 查看完整帮助
git 命令 --help        # 同上
git 命令 -h            # 简短帮助
man git-命令           # 手册页
```

---

**提示**：不要试图记住所有命令！掌握常用命令，其他用到了再查。

需要帮助？记住：
```bash
git help
```
