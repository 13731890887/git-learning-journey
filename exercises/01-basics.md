# 练习 1：Git 基础操作

## 任务清单

按照以下步骤完成基础练习：

### 步骤 1：初始化仓库
```bash
git init
```

### 步骤 2：检查状态
```bash
git status
```
你应该看到所有文件都是 "untracked" 状态。

### 步骤 3：配置用户信息（如果还没配置）
```bash
git config --global user.name "你的名字"
git config --global user.email "你的邮箱"
```

### 步骤 4：添加文件到暂存区
```bash
git add README.md
```

### 步骤 5：再次检查状态
```bash
git status
```
现在 README.md 应该在 "Changes to be committed" 中。

### 步骤 6：提交
```bash
git commit -m "首次提交：添加项目说明"
```

### 步骤 7：查看提交历史
```bash
git log --oneline
```

### 步骤 8：添加剩余文件
```bash
git add .
git commit -m "添加练习文件和示例"
```

---

## ✅ 完成标志

当你完成以上所有步骤后，你应该：
- 有 2 次提交记录
- `git status` 显示工作区是干净的（nothing to commit）

## 🤔 思考题

1. `git add .` 和 `git add 文件名` 有什么区别？
2. 如果不写 `-m` 参数会发生什么？
3. `git status` 显示的红色和绿色文件名有什么区别？
