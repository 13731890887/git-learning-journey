# 练习 3：GitHub 远程仓库

## 任务清单

### 步骤 1：在 GitHub 创建仓库

1. 打开 https://github.com
2. 点击右上角 `+` → `New repository`
3. 仓库名：`git-learning-journey`（或你喜欢的名字）
4. 选择 Public 或 Private
5. **不要**勾选任何初始化选项
6. 点击 `Create repository`

### 步骤 2：连接远程仓库

GitHub 创建后会显示命令，找到类似这样的：

```bash
git remote add origin https://github.com/你的用户名/仓库名.git
```

或者使用 SSH（如果你配置了 SSH 密钥）：
```bash
git remote add origin git@github.com:你的用户名/仓库名.git
```

### 步骤 3：查看远程仓库
```bash
git remote -v
```

### 步骤 4：推送到 GitHub
```bash
git push -u origin main
```

💡 **参数解释**：
- `-u`：设置上游分支，之后只需 `git push`
- `origin`：远程仓库的默认名称
- `main`：要推送的分支

### 步骤 5：在 GitHub 网页查看

打开你的仓库 URL，你应该看到：
- README.md 的内容
- 提交历史
- 所有文件

### 步骤 6：在 GitHub 上编辑文件

1. 在 GitHub 上点击任意文件
2. 点击右上角的铅笔图标 ✏️
3. 修改内容
4. 在底部填写提交信息
5. 点击 `Commit changes`

### 步骤 7：从 GitHub 拉取更新
```bash
git pull
```

### 步骤 8：本地修改并推送
```bash
# 本地修改
echo "新的练习" >> exercises/03-github.md

# 提交
git add .
git commit -m "添加新练习"

# 推送
git push
```

---

## ✅ 完成标志

- 成功推送代码到 GitHub
- 在 GitHub 网页看到你的代码
- 能够从 GitHub 拉取更新
- 能够推送本地修改

## 🌟 挑战任务

1. 在 GitHub 上创建一个新分支
2. 在不同分支间切换
3. 在 GitHub 上创建 Pull Request
4. 合并 Pull Request

## 💡 常见问题

### Q: 推送时提示 "fatal: remote origin already exists"
**A:** 你已经添加过远程仓库，可以使用：
```bash
git remote set-url origin 新的URL
```

### Q: 推送时要求输入用户名和密码
**A:** 推荐配置 SSH 密钥，或使用 GitHub Personal Access Token。

### Q: 提示 "failed to push some refs"
**A:** 远程有本地没有的更新，先 `git pull` 再 `git push`。
